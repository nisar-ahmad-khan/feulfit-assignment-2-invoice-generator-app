<div class="max-w-3xl mx-auto mt-10 space-y-6">

  <!-- Notice -->
  <% if notice.present? %>
    <div class="bg-green-100 text-green-700 px-4 py-2 rounded-md">
      <%= notice %>
    </div>
  <% end %>

  <!-- Line Item Card -->
  <div class="bg-white shadow-md rounded-xl p-6">
    <%= render @line_item %>
  </div>

  <!-- Action Buttons -->
  <div class="flex flex-wrap gap-4 mt-4">
    <%= link_to "Edit this line item", edit_line_item_path(@line_item),
      class: "bg-indigo-600 hover:bg-indigo-700 text-white font-medium px-4 py-2 rounded-md transition duration-200" %>

    <%= link_to "Back to line items", line_items_path,
      class: "bg-gray-200 hover:bg-gray-300 text-gray-800 font-medium px-4 py-2 rounded-md transition duration-200" %>

    <%= button_to "Destroy this line item", @line_item,
      method: :delete,
      data: { confirm: "Are you sure?" },
      class: "bg-red-600 hover:bg-red-700 text-white font-medium px-4 py-2 rounded-md transition duration-200" %>
  </div>

</div>
